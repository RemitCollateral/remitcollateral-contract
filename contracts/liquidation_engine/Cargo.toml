[package]
name = "rc-liquidation-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
rc-guarantor-vault = { path = "../guarantor_vault" }
rc-loan-ledger = { path = "../loan_ledger" }
